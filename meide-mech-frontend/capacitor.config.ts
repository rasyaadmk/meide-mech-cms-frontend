import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'meide-mech-frontend',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
